pub mod decoder;
pub mod types;
pub mod errors;
pub mod feed_clients;
pub mod feed_client;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
